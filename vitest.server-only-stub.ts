// Stub vazio para o pacote "server-only" durante os testes (Vitest).
// Em runtime do Next, "server-only" impede import em Client Components; nos
// testes isso não se aplica, então o substituímos por um no-op.
export {};
